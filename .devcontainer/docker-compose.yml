version: "3.9"
services:
    scrapy:
        image: continuumio/anaconda3
        container_name: jl_scrapy
        ports:
            - 18888:8888
        volumes:
            - "../:/opt/scrapy"
        tty: true
        external_links:
            - "gitlab:gitlab.local"
networks:
    default:
        external: true
        name: dev_network
